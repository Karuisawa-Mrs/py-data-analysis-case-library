# Data README

## Data Mode
Simulated: synthetic network graph generated by `analysis.py`.

## Schema
- Nodes and edges stored as CSV with `node_id`, `neighbor_id`, `weight`.

## Acquisition
Generated internally via `networkx` stochastic block model with fixed seed.
