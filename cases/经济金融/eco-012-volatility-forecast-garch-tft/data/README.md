# Data README

## Data Mode
Simulated: synthetic volatility series generated by `analysis.py`.

## Schema
- `date`: datetime
- `returns`: float, simulated asset returns
- `realized_vol`: float, squared returns as proxy

## Acquisition
Internally generated via GARCH-like DGP with `np.random.default_rng(seed)`.
